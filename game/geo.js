geo = function(points){
  this.points = points;
  this.boundingVertices;
}
geo.prototype ={
  constructor: geo,
  makeBoundingVertices: function(){
    points=this.points;
    for(var i=0;i<points.length;i++){
      
    }
  }
}